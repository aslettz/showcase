import {Asset} from "../components/infoSection";

const HOME_TEXT = '';
const HOME_HEADER = 'Welcome to my portfolio!';

const HOME: Asset = {
    header: HOME_HEADER,
    body: HOME_TEXT,
    imgPath: "",
};

export default HOME;